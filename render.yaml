services:
  - type: web
    name: spring-boot-demo
    env: java
    plan: free
    buildCommand: ./mvnw clean package -DskipTests
    startCommand: java -jar target/*.jar
    envVars:
      - key: JAVA_VERSION
        value: 17
    # Optional: Auto-deploy from specific branch
    branch: main